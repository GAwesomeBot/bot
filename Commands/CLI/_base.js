/**
 * This is a full documented base file for CLI commands
 * Use this as a base for coding
 */
module.exports = async (main, cmdData, args) => {
	/**
	 * @type {Object}
	 * @param main The object containing the most important things.
	 * You can deconstruct it using { Value }
	 * @property {CLI} cli The CLI client. Look in /Modules/CLI.js for available properties.
	 */
	/**
	 * @type {Object}
	 * @param cmdData The command data
	 * @property {?String} description The command description
	 * @property {?String} usage The command usage
	 * @property {?Boolean} isMultiline Indicates whether the command accepts input with more than one line.
	 */
	/**
	 * @type {String}
	 * @param args The command arguments
	 */
};
